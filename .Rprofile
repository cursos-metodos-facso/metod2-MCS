source("renv/activate.R")
.libPaths("renv/library/r-4.2/x86_64-w64-mingw32")
# Run the global .Rprofile if it exists
if (file.exists("~/.Rprofile")) {
  base::sys.source("~/.Rprofile", envir = environment())
}
